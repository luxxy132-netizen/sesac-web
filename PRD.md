# PRD — 서울 아파트 실거래 대시보드

**작성일** 2026-08-12
**대상 배포** GitHub Pages (`https://luxxy132-netizen.github.io/sesac-web/`)
**디자인 기준** Montage (`@wanteddev/wds`) 토큰, 포인트 컬러 red

---

## 1. 배경

이미 서울 전역 아파트 실거래 데이터(32만 건, 12개월치)를 확보했으나, 현재는 파이썬 스크립트를 돌려 콘솔로 상위 5건을 출력하는 수준이다. 이 데이터를 중개 실무에서 바로 꺼내 쓸 수 있는 웹 대시보드로 만든다.

## 2. 페르소나

**부동산 중개 실무자**

- 고객 상담 중 "요즘 시세 어때요?"라는 질문을 매일 받는다.
- 답을 감이 아니라 **숫자와 그래프로** 보여주고 싶다.
- 담당 지역이 있지만 고객이 다른 구를 물어보는 경우도 잦다.
- 사무실 PC에서 주로 보지만, 현장에서 폰으로 고객에게 화면을 보여주는 상황도 있다.

### 사용자 시나리오

1. **아침 시세 체크** — 출근해서 대시보드를 연다. 담당 구의 지난달 대비 등락을 확인하고 오늘 상담 톤을 잡는다.
2. **고객 상담** — "은평구 지금 오르는 중인가요?" → 구를 선택하고 12개월 추이 차트를 보여준다.
3. **지역 비교** — "마포랑 은평 중에 어디가 나아요?" → 구별 랭킹에서 두 구의 평당가와 상승률을 비교한다.
4. **단지 확인** — 고객이 특정 단지를 언급한다 → 단지명으로 검색해 거래 건수와 시세대를 확인한다.

## 3. 목표 / 비목표

### 목표

- 대시보드를 열고 **10초 안에** "지금 시장이 오르는지 내리는지" 판단할 수 있다.
- 매매·전세·월세를 한 화면에서 전환하며 볼 수 있다.
- PC와 모바일 모두에서 읽을 수 있다.

### 비목표 (이번 범위 아님)

- 실시간 데이터 연동 (정적 스냅샷 데이터를 사용)
- 사용자 계정, 즐겨찾기 저장, 알림
- 시세 예측 / 투자 추천
- 지도 기반 시각화 (좌표 데이터 없음)

## 4. 데이터 명세

**원본** `seoul-apt-latest.csv` — 320,141행, cp949 인코딩, 23MB

| 컬럼 | 설명 |
|---|---|
| `contract_ym` | 계약 연월 |
| `contract_date` | 계약일 |
| `gu` | 자치구 (25개) |
| `dong` | 법정동 (340개) |
| `complex` | 단지명 (8,216개) |
| `area_m2` | 전용면적 |
| `floor` | 층 |
| `deal_type` | 매매 / 전세 / 월세 |
| `price` | 매매가 (만원) |
| `deposit` | 보증금 (만원) |
| `monthly_rent` | 월세 (만원) |
| `price_per_pyeong` | 평당가 (만원) |

**기간** 2025-07 ~ 2026-06 (12개월)

**거래 유형별 건수** 매매 72,459 / 전세 129,649 / 월세 118,033

### ⚠️ 데이터 제약 (구현 시 반드시 반영)

| 제약 | 내용 | 대응 |
|---|---|---|
| **평당가 결측** | `price_per_pyeong`은 **매매에만** 채워져 있음. 전세·월세는 전부 빈 값 | 전월세 평당가는 `deposit / (area_m2 / 3.3058)`로 직접 계산 |
| **금액 컬럼 분리** | 매매는 `price`, 전세·월세는 `deposit`에 값이 들어감 | 거래 유형에 따라 참조 컬럼을 분기 |
| **월세 이중 지표** | 월세는 보증금과 월 임대료 두 값을 가짐 | 월세 탭에서는 보증금 기준 지표 + 월세 중위값을 별도 표기 |
| **인코딩** | 파일이 UTF-8이 아닌 **cp949** | 전처리 시 `encoding="cp949"` 고정 |

## 5. 기능 요구사항

### F1. 시장 요약 (최우선)

화면 최상단에 현재 시장 상태를 요약한 KPI 카드.

- 선택된 구·거래유형 기준 **최근월 중위가격**, **최근월 중위 평당가**, **거래량**
- 각 지표에 **전월 대비 등락률** 표시 (상승 red / 하락 blue / 보합 gray)
- 기본 선택: 서울 전체, 매매

### F2. 추이 차트

12개월 시계열 라인 차트.

- **평당가 추이**와 **중위가격 추이** 두 지표 (인터뷰에서 선택된 핵심 지표)
- 거래량은 보조 축 또는 별도 막대로 병기
- 구 선택 시 해당 구 시계열로 갱신
- 거래유형 탭(매매/전세/월세)으로 전환

### F3. 구별 랭킹

25개 구를 한눈에 비교.

- 정렬 기준: 평당가 / 중위가격 / 거래량 / 전월 대비 등락률
- 등락률은 색상 강도로 표현 (히트맵 성격)
- 행 클릭 시 해당 구가 F1·F2에 반영

### F4. 단지명 검색

- 단지명 부분 일치 검색 (한글 입력 중 즉시 반영)
- 결과: 단지명, 구·동, 거래 건수, 중위 평당가, 중위 거래가, 최근 거래일
- 8,216개 단지 대상

### F5. 반응형 레이아웃

- **PC (≥1024px)**: KPI 4열, 차트와 랭킹 2단 배치
- **태블릿 (768~1023px)**: KPI 2열, 세로 스택
- **모바일 (<768px)**: 전부 1열, 차트 가로 스크롤 허용

## 6. 지표 정의

| 지표 | 정의 | 비고 |
|---|---|---|
| 중위가격 | 해당 월·구·유형 거래 금액의 중위값 | 평균은 고가 거래에 휘둘리므로 중위값 사용 |
| 중위 평당가 | 평당가의 중위값 (만원/평) | 평 = `area_m2 / 3.3058` |
| 거래량 | 해당 월·구·유형 거래 건수 | |
| 전월비 | `(최근월 - 직전월) / 직전월 × 100` | 소수점 1자리 |
| 전년비 | 12개월 데이터 특성상 **첫 달 대비 최근 달** 변화로 대체 표기 | "12개월 누적 변화"로 라벨링 |

## 7. 기술 아키텍처

GitHub Pages는 정적 호스팅이므로 서버가 없다. 23MB CSV를 브라우저에서 파싱하는 것은 불가능하므로 **빌드 타임 전처리**로 해결한다.

```
seoul-apt-latest.csv (23MB, cp949)
        │
        ▼  build_data.py  (파이썬 표준 라이브러리)
        │
   ┌────┴────┐
   ▼         ▼
trend.json   complexes.json
(~100KB)     (~600KB, 지연 로드)
        │
        ▼
   index.html + assets/  →  GitHub Pages
```

### 산출 데이터

**`data/trend.json`** — 초기 로드용, 대시보드 핵심
- 월 × 구(25개 + 서울전체) × 거래유형(3) 사전 집계
- 각 레코드: 중위가격, 중위 평당가, 거래량
- 약 936 레코드 → 파일 100KB 미만

**`data/complexes.json`** — 단지 검색용, 검색창 첫 사용 시 지연 로드
- 단지별: 단지명, 구, 동, 거래 건수, 중위 평당가, 중위 거래가, 최근 거래일
- 8,216 레코드

### 프론트엔드

- **순수 HTML/CSS/JS** (빌드 도구 없음 — 현재 프로젝트 구조 유지)
- 차트: 의존성 없이 **인라인 SVG로 직접 렌더링** (외부 CDN 미사용, 오프라인·CSP 안전)
- 디자인: 기존 `index.html`의 Montage 토큰 CSS 변수를 그대로 승계

### 파일 구조

```
index.html            대시보드 (메인)
about.html            기존 자기소개 페이지 (분리 이전)
assets/dashboard.css
assets/dashboard.js
data/trend.json
data/complexes.json
build_data.py         CSV → JSON 전처리
analyze.py            기존 스크립트 (유지)
```

## 8. 배포

- `main` 브랜치 `/ (root)` → GitHub Pages 자동 배포 (이미 활성화됨)
- 원본 CSV(23MB)는 저장소에 이미 커밋되어 있으며 그대로 유지 (재현성 목적)
- `build_data.py`는 수동 실행. 데이터 갱신 시 재실행 후 JSON 커밋

## 9. 성공 기준

| 항목 | 기준 |
|---|---|
| 초기 로딩 | 첫 화면 렌더까지 2초 이내 (trend.json만 로드) |
| 판단 속도 | 대시보드 진입 후 10초 내 시장 방향 파악 가능 |
| 반응형 | 375px ~ 1920px 구간에서 가로 스크롤 없이 표시 |
| 데이터 정합성 | 대시보드 수치가 CSV 원본 재계산 결과와 일치 |
| 의존성 | 외부 런타임 의존성 0개 |

## 10. 향후 과제

- 면적대별 분석 (소형/중형/대형 평형 비교)
- 최근 실거래 리스트 (구 선택 시 개별 거래 내역)
- 전세가율 (전세 중위가 / 매매 중위가)
- 데이터 자동 갱신 (GitHub Actions + 공공데이터 API)
